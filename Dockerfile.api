FROM node:22-bookworm-slim
WORKDIR /app
RUN corepack enable
COPY . .
RUN pnpm install --frozen-lockfile=false
RUN pnpm --filter @exam/api build
ENV NODE_ENV=production
EXPOSE 3000
CMD ["pnpm", "--filter", "@exam/api", "exec", "tsx", "src/server.ts"]
