import { useEffect,useState } from 'react';import { adminApi } from '../../infrastructure/api/auth';
export function AdminSecurityPage(){const[tab,setTab]=useState<'events'|'alerts'|'audit'|'system'|'features'>('events');const[events,setEvents]=useState<any[]>([]);const[alerts,setAlerts]=useState<any[]>([]);const[audit,setAudit]=useState<any[]>([]);const[system,setSystem]=useState<any>();const[flags,setFlags]=useState<any[]>([]);const[search,setSearch]=useState('');const[severity,setSeverity]=useState('');const[page,setPage]=useState(1);const load=()=>adminApi.security(page,50,`${search?`&search=${encodeURIComponent(search)}`:''}${severity?`&severity=${severity}`:''}`).then(r=>setEvents(r.events)).catch(()=>setEvents([]));useEffect(()=>{if(tab==='events')void load();if(tab==='alerts')void adminApi.alerts(page).then(r=>setAlerts(r.alerts));if(tab==='audit')void adminApi.audit(page).then(r=>setAudit(r.logs));if(tab==='system')void adminApi.system().then(setSystem);if(tab==='features')void adminApi.features().then(r=>setFlags(r.flags))},[tab,page]);return <div className="page-stack max-w-7xl mx-auto"><section className="page-hero"><div><span className="eyebrow">ADMIN</span><h1>Security center</h1><p>Events, alerts, audit, feature flags và system health.</p></div></section><div className="flex flex-wrap gap-2"><button className="btn-secondary" onClick={()=>setTab('events')}>Events</button><button className="btn-secondary" onClick={()=>setTab('alerts')}>Alerts</button><button className="btn-secondary" onClick={()=>setTab('audit')}>Audit</button><button className="btn-secondary" onClick={()=>setTab('system')}>System</button><button className="btn-secondary" onClick={()=>setTab('features')}>Feature flags</button></div>{tab==='events'&&<><div className="card flex gap-2"><input className="input flex-1" placeholder="Search event / user / IP / endpoint" value={search} onChange={e=>setSearch(e.target.value)}/><select className="select" value={severity} onChange={e=>setSeverity(e.target.value)}><option value="">Severity</option><option>INFO</option><option>WARNING</option><option>HIGH</option><option>CRITICAL</option></select><button className="btn-primary" onClick={()=>{setPage(1);void load()}}>Search</button></div><Table rows={events} cols={['createdAt','severity','action','username','userId','ip','userAgent','endpoint','result']}/></>}{tab==='alerts'&&<div className="card overflow-auto"><table className="w-full text-sm"><thead><tr><th className="p-3 text-left">Time</th><th className="p-3 text-left">Severity</th><th className="p-3 text-left">Type</th><th className="p-3 text-left">Account/IP</th><th className="p-3 text-left">Reason</th><th className="p-3">Status</th></tr></thead><tbody>{alerts.map(a=><tr className="border-t" key={a.id}><td className="p-3">{new Date(a.createdAt).toLocaleString('vi-VN')}</td><td className="p-3">{a.severity}</td><td className="p-3">{a.type}</td><td className="p-3">{a.userId??a.ip??'—'}</td><td className="p-3">{a.reason}</td><td className="p-3"><select className="select" value={a.status} onChange={e=>void adminApi.updateAlert(a.id,e.target.value).then(()=>adminApi.alerts(page)).then(r=>setAlerts(r.alerts))}><option>NEW</option><option>REVIEWED</option><option>RESOLVED</option></select></td></tr>)}</tbody></table></div>}{tab==='audit'&&<Table rows={audit} cols={['createdAt','adminId','action','targetId','ip','result']}/>} {tab==='system'&&<div className="stat-grid"><Stat k="API" v={system?.api?.status}/><Stat k="Database" v={system?.database?.status}/><Stat k="Storage" v={system?.storage?.status}/><Stat k="Sync" v={system?.sync?.status}/><Stat k="Latency" v={system?.latencyMs!=null?`${system.latencyMs} ms`:undefined}/><Stat k="Uptime" v={system?.api?.uptimeSeconds!=null?`${system.api.uptimeSeconds}s`:undefined}/></div>} {tab==='features'&&<div className="card space-y-2">{flags.map(f=><div key={f.key} className="flex items-center justify-between border rounded-xl p-3"><div><b>{f.key}</b><div className="text-xs text-slate-400">Updated {new Date(f.updatedAt).toLocaleString('vi-VN')}</div></div><button className={`btn-secondary ${f.enabled?'':'text-red-600'}`} onClick={()=>void adminApi.setFeature(f.key,!f.enabled).then(()=>adminApi.features()).then(r=>setFlags(r.flags))}>{f.enabled?'ON':'OFF'}</button></div>)}</div>}<div className="flex justify-between"><button className="btn-secondary" disabled={page<=1} onClick={()=>setPage(page-1)}>Trước</button><button className="btn-secondary" onClick={()=>setPage(page+1)}>Sau</button></div></div>}
function Table({rows,cols}:{rows:any[];cols:string[]}){return <div className="card overflow-auto"><table className="w-full text-sm"><thead><tr>{cols.map(c=><th key={c} className="p-3 text-left text-slate-400">{c}</th>)}</tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t border-slate-100">{cols.map(c=><td key={c} className="p-3">{c==='createdAt'?new Date(r[c]).toLocaleString('vi-VN'):r[c]??'—'}</td>)}</tr>)}</tbody></table></div>}
function Stat({k,v}:{k:string;v:any}){return <div className="stat-card"><div><strong>{v??'—'}</strong><span>{k}</span></div></div>}
