import { useEffect,useState } from 'react';import { adminApi } from '../../infrastructure/api/auth';import { importExam } from '@exam/exam-format';
export function AdminExamsPage(){const [rows,setRows]=useState<any[]>([]);const [error,setError]=useState('');const [busy,setBusy]=useState(false);const load=()=>adminApi.officialList().then(r=>setRows(r.exams)).catch(e=>setError(e instanceof Error?e.message:'Không thể tải đề'));useEffect(()=>{void load()},[]);
 async function upload(file:File){setBusy(true);setError('');try{if(file.name.toLowerCase().endsWith('.json')){const content=JSON.parse(await file.text());const preview=content?.content??content;await adminApi.createOfficial({content:preview});}else{const bytes=new Uint8Array(await file.arrayBuffer());const imported=await importExam(bytes);let b='';const chunk=0x8000;for(let i=0;i<bytes.length;i+=chunk)b+=String.fromCharCode(...bytes.subarray(i,i+chunk));const packageBase64=btoa(b);await adminApi.createOfficial({id:imported.content.id,title:imported.content.title,subject:imported.content.subject,grade:imported.content.grade,version:imported.content.version,contentHash:imported.contentHash,packageBase64,questionCount:imported.content.questions.length});}await load()}catch(e){setError(e instanceof Error?e.message:'Đề không hợp lệ')}finally{setBusy(false)}}
 async function remove(id:string){if(!window.confirm('Xóa Official Exam? Đề sẽ bị unpublish; các bản đã tải về không tự động bị xóa.'))return;await adminApi.deleteOfficial(id);await load()}
 async function publish(id:string,published:boolean){await adminApi.updateOfficial(id,{published});await load()}
 return <div className="page-stack max-w-7xl mx-auto"><section className="page-hero"><div><span className="eyebrow">ADMIN</span><h1>Official Exams</h1><p>Import .exam hoặc JSON theo format hiện tại; server validate schema/hash trước khi lưu.</p></div><label className="btn-primary cursor-pointer">{busy?'Đang xử lý…':'Import .exam / JSON'}<input className="hidden" type="file" accept=".exam,.json,application/json" disabled={busy} onChange={e=>{const f=e.target.files?.[0];if(f)void upload(f)}}/></label></section>{error&&<p className="form-error">{error}</p>}<div className="card overflow-auto"><table className="w-full text-sm"><thead><tr className="text-left text-slate-400"><th className="p-3">Title</th><th className="p-3">Subject</th><th className="p-3">Version</th><th className="p-3">Status</th><th className="p-3">Action</th></tr></thead><tbody>{rows.map(e=><tr key={e.id} className="border-t border-slate-100"><td className="p-3 font-semibold">{e.title}</td><td className="p-3">{e.subject}</td><td className="p-3">v{e.version}</td><td className="p-3">{e.deletedAt?'Unpublished':'Published'}</td><td className="p-3 flex gap-2">{e.deletedAt?<button className="btn-secondary" onClick={()=>void publish(e.id,true)}>Publish</button>:<button className="btn-secondary" onClick={()=>void publish(e.id,false)}>Unpublish</button>}<button className="btn-secondary text-red-600" onClick={()=>void remove(e.id)}>Xóa</button></td></tr>)}</tbody></table></div></div>}
