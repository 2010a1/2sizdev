import { FormEvent, useState } from 'react';
import { Link, useNavigate } from 'react-router-dom';
import { authApi } from '../../infrastructure/api/auth';
import { useAuthStore } from '../../state/authStore';
export function LoginPage(){const nav=useNavigate();const setUser=useAuthStore(s=>s.setUser);const [username,setUsername]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{const r=await authApi.login(username,password);setUser(r.user);nav(r.user.mustChangePassword?'/account':'/library')}catch(e){setError(e instanceof Error?e.message:'Không thể đăng nhập')}finally{setBusy(false)}}return <div className="min-h-screen grid place-items-center p-4 bg-slate-50"><form onSubmit={submit} className="card w-full max-w-md p-7 space-y-4"><div><span className="eyebrow">TÀI KHOẢN</span><h1 className="mt-2 text-2xl font-extrabold">Đăng nhập</h1><p className="mt-2 text-sm text-slate-500">Đăng nhập bằng username và mật khẩu. Dữ liệu học tập vẫn được lưu offline trên thiết bị.</p></div><input className="input" placeholder="Username" value={username} onChange={e=>setUsername(e.target.value)} autoComplete="username"/><input className="input" type="password" placeholder="Mật khẩu" value={password} onChange={e=>setPassword(e.target.value)} autoComplete="current-password"/>{error&&<p className="form-error">{error}</p>}<button className="btn-primary w-full" disabled={busy}>{busy?'Đang đăng nhập…':'Đăng nhập'}</button><p className="text-sm text-slate-500">Chưa có tài khoản? <Link className="font-semibold text-indigo-600" to="/register">Đăng ký</Link></p></form></div>}
